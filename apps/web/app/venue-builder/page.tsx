import { VenueBuilder } from '../../components/venue-builder/VenueBuilder';

export default function VenueBuilderPage() {
  return <VenueBuilder />;
}

export const metadata = {
  title: 'NOIR Venue Builder',
};
